# Redis Cluster Configuration
CLUSTER_ARGS=--cluster-enabled yes --cluster-config-file nodes.conf --cluster-node-timeout 5000 --cluster-replica-validity-factor 0 --cluster-migration-barrier 1 --cluster-require-full-coverage no --appendonly yes --bind 0.0.0.0 --protected-mode no